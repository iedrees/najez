<div class="flex items-center">
    <img src="{{asset('images/logo.svg')}}" alt="{{ config('app.name') }}" {{ $attributes->merge(['class' => 'mx-auto']) }}>
    <h1 class="mx-3 font-bold">{{ config('app.name') }}</h1>
</div>
